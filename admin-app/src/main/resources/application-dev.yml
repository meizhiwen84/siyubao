spring:
  devtools:
    restart:
      enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  file:
    path: ${SIYUBAO_ADMIN_LOG_DIR:./logs}
  level:
    root: INFO

sxjw:
  seed:
    mode: DEV
