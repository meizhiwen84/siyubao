spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  file:
    path: ${SIYUBAO_USER_LOG_DIR:./logs}
  level:
    root: INFO

admin:
  base-url: ${SIYUBAO_ADMIN_BASE_URL:http://localhost:6943}
